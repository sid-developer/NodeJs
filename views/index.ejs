<!DOCTYPE html>
<html lang="en">

<%- include('./partials/head') %>

<body>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>


    <%- include('./partials/nav') %>

    <div class="container">
        <ul class="collection">
            <% if(items.length > 0) { %>
                <% items.forEach(element => { %>
                    <li class="collection-item"><%= element.name %> - <%= element.mob %></li>
                <% }) %>

                
            <% } else { %> 
                <li class="collection-item">No data available</li>
            <% } %>
        </ul>
    </div>
    
</body>
</html>
afaac389b90cb0449b6e9146622a4e27